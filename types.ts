export type FeatureCardProps = {
  icon: any;
  title: string;
  content: string;
};

export type BtnProps = {
  label: string;
  className?: string;
};

export type FeedbackCardProps = {
  content: string;
  name: string;
  title: string;
  img: any;
};

export type IconProps = {
  icon: any;
  Icon?: JSX.Element;
  className?: string;
};
